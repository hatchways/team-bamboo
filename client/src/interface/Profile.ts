export interface Profile {
  coverPhoto: string;
  photo: string;
  uploadedImages: string[];
  name: string;
  description: string;
  hourlyRate: number;
  availability: boolean[];
  city: string;
  averageRating: number;
}
